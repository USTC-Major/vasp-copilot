# 首版基础计算与结果取回

本指南对应当前候选分支的手工流程。它帮助用户从结构生成基础输入包，在 Toolbox 中准备和提交一个计算，再按需下载三个必要结果文件。真实 VASP 计算仍取决于合法 POTCAR、用户核对的脚本、站点配置和实际作业状态。

1. 打开 `/workflow`，上传带真实晶格与完整原子坐标的 POSCAR 或 CIF。核对结构摘要和参数，仅选择所需的静态计算，查看最终确认后生成并下载 ZIP。
2. 将 ZIP **手工**解压到运行 8000 后端的机器或容器可读写目录；浏览器电脑的下载目录若未映射给后端，Toolbox 不能直接读取。检查 `02_static/POSCAR`、`INCAR`、`KPOINTS`、`INPUT_CHECK_REPORT.md`。包中**不含 POTCAR**。用户须按授权与元素顺序自行提供已经准备好的 POTCAR，并核对/替换模板 `submit.sh` 为站点可用脚本。产品不自动解压 ZIP、不拼接 POTCAR，也不证明输入的科学适用性。
3. 打开 `/toolbox/projects`，新建项目和任务。本地工作区填上述解压目录；真实远端运行还要填超算工作区，并在 `/toolbox/settings` 配置严格已知主机密钥的 SSH 与调度目标。保存单步计划，标识设为 `02_static`，与 ZIP 中目录一致。扫描并登记现有输入；若需要上传，逐个确认具体来源、目标和哈希，直到远端所需文件齐备。没有配置远端时可继续本地准备，但不能声称已提交。
4. 在任务页选择当前 job/attempt，认领用户脚本并批准指纹卡，执行硬预检；有缺项须先修复。预检通过后请求提交确认，人工批准**这一次**计算。以真实回执中的 `submission_state=submitted` 和调度器作业号为准，按钮响应并不等于计算完成。任务监控和报告来自当前 Toolbox 状态；无需启用 AI 模型。
5. 作业达到 `completed`、`failed` 或 `not_converged` 后，在同一任务页“计算结果取回”选定已结束的 job，分别点击“下载 OUTCAR / OSZICAR / CONTCAR”。单文件最大 32 MiB；超限、缺失或读取中变化时明确失败，不返回截断文件。还可保存当前任务的确定性报告 Markdown。WAVECAR、CHGCAR、POTCAR、任意路径和全目录下载不在此入口。小文本预览仍限 12,000 字节。

下载读取的是**该次提交记录绑定的目录中当前存在的文件**。接口按当前 job/attempt、提交授权、调度目标和严格 known_hosts 核对，并在整包接收后验证长度与 SHA-256；这只能证明本次传输完整，不能证明目录里的每个文件都是该次计算生成，更不能证明科学收敛。尤其失败作业目录可能保留旧 CONTCAR，用户应结合时间、报告和站点记录判断。历史提交记录未保存当时真实 host-key 指纹，因此当前的严格主机校验不能追溯证明历史指纹相同。若要分析结果，可按诊断页要求自行上传已取得的结果包；Toolbox 不自动送文件给 AI 或诊断服务。
