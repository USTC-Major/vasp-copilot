#!/bin/bash
#SBATCH --job-name={{ job_name }}
#SBATCH --nodes={{ settings.nodes }}
#SBATCH --ntasks-per-node={{ settings.tasks_per_node }}
#SBATCH --time={{ settings.walltime }}
{% if settings.partition %}#SBATCH --partition={{ settings.partition }}
{% endif %}{% if settings.account %}#SBATCH --account={{ settings.account }}
{% endif %}{% if settings.module_loads %}{% for module in settings.module_loads %}module load {{ module }}
{% endfor %}{% endif %}
# Generated by VASP-Doctor BE-A ({{ profile.scheduler_type }} profile).
# 本脚本仅为生成产物；提交由用户在集群上手动执行：{{ profile.submit_command_hint }}

{{ profile.launcher_prefix }} {{ settings.vasp_binary_hint }}
