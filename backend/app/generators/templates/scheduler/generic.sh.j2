#!/bin/bash
# Generic run script（无调度系统指令；适用于本地/手动运行场景）
# Generated by VASP-Doctor BE-A ({{ profile.scheduler_type }} profile).
# 运行方式：{{ profile.submit_command_hint }}
set -euo pipefail
{% if settings.module_loads %}{% for module in settings.module_loads %}module load {{ module }}
{% endfor %}{% endif %}
# 资源说明（仅记录，不强制执行）：nodes={{ settings.nodes }}, tasks_per_node={{ settings.tasks_per_node }}, walltime={{ settings.walltime }}

{{ profile.launcher_prefix }} -np {{ total_tasks }} {{ settings.vasp_binary_hint }}
